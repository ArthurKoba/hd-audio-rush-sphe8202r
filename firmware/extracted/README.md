# Extracted Sunplus modules

Canonical STK extraction archive:

`modules.tar`

Archive SHA-256:
`542012b5b31ba01ab260e6f75a3f2dcfd8362e89b2143e0eca15279ccd23d98a`

The archive contains all 18 STK module slots, including zero-length slots. The same byte-exact files are materialized under `modules/` for direct Ghidra and offline analysis.

See `SHA256SUMS`, `firmware/MANIFEST.md` and `docs/firmware.md`.
