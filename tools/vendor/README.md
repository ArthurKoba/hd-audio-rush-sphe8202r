# Vendor tools

This directory contains preserved third-party tools needed to reproduce the reverse-engineering workflow.

Vendor artifacts are evidence/dependencies, not project-owned source code. Preserve them byte-for-byte and document provenance, version and SHA-256.
