# Reverse engineering workspace

Canonical analysis notes live in `docs/`. This directory is for reproducible reverse assets such as import maps, helper scripts, symbol maps and Ghidra automation.

Do not commit mutable Ghidra project databases.
